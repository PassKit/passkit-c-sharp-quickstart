namespace Quickstart.Common
{
    public static class Constants
    {
        public const string Environment = "pub1";
        public const string AppleCertificate = "";

        /* 
         * Add a valid email address if you wish to receive welcome emails from the passes you create.
         * Ensure any address is valid to avoid having your projects blocked from sending email due to a high bounce rate.
         * Do not drag emails into the spam folder to avoid being blocked for a high complaint rate.
         */
        public const string EmailAddress = "";
    }
}

